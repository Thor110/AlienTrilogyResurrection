// ALTFormats
//
// This will hold the C++ port of the BND/B16 section parsing, palette
// handling, model/UV extraction and level (MAP) parsing currently living
// in ALTViewer.cs / ModelRenderer.cs / DetectDimensions.cs etc.
//
// Deliberately empty for now - the boot/locate/patch stage doesn't need it
// yet, and it's better to port the format readers deliberately once, with
// tests against known files, rather than dragging them in piecemeal.
